[print_schema]
# This will cause only the users and posts tables to be output
filter = { only_tables = ["r_posts"] }

# This will cause all tables *except* the comments table to be
# output
#filter = { except_tables = ["accounts"] }